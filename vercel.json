{
  "buildCommand": "tsc -b && vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://lora-backend-production.up.railway.app/api/:path*"
    }
  ]
}
